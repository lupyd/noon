[workspace]
members = [
    "crates/core",
    "crates/server",
]

resolver = "3"



[workspace.dependencies]
noon-core = { path = "crates/core" }
noon-server = { path = "crates/server" }



[profile.release]
strip = true
opt-level = 3
panic = "abort"
lto = true
codegen-units = 1

